import {mapStateProps} from './App';
import {fromJs} from 'immutable';

descripe('mapStateProps', () => {
    // test("should return the right object", () => {
    //     const state = fromJs ({
    //         isLoggedIn: true
    //     });
    //     const expectedProps = { isLoggedIn: true};
    //     expect(mapStateProps(state)).toEqual(expectedProps);
    // });
});